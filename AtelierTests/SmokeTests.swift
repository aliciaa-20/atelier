import Testing

/// Phase 0 placeholder. Its only job is to prove the test target builds, links
/// against the app, and runs — so that when Phase 1 starts test-driving
/// `NotchGeometry`, a red test means a real failure and not a broken harness.
@Test func testHarnessRuns() {
    #expect(Bool(true))
}
